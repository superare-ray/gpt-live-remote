port: 7880
log_level: info

rtc:
  tcp_port: 7881
  udp_port: 7882
  use_external_ip: true

room:
  empty_timeout: 60
  departure_timeout: 20

# TURN/TLS is added after a dedicated domain and trusted certificate are available.
# The first end-to-end test uses ICE/UDP with ICE/TCP fallback.
